[project]
name = "my-geo-project"
version = "0.1.0"
description = "geophysics projects library"
authors = [
    {name = "Fyodor Knyazkov"}
]
license = "MIT"
requires-python = ">=3.13"
dependencies = [
    "numpy (>=2.5.3,<3.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
